<form {{action 'authenticate' on='submit'}}>
	{{input id='identification' placeholder='Login' value=identification}}
	{{input id='password' placeholder='Mot de passe' type='password' value=password}}
	<button type="submit">Connexion</button>
	{{#if errorMessage}}
		<p>{{errorMessage}}</p>
	{{/if}}
</form>
